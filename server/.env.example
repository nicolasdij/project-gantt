# Copia a .env para desarrollo local fuera de Docker.
# Dentro de Docker Compose estas variables las inyecta docker-compose.yml.
DATABASE_URL="postgresql://gantt:gantt@localhost:5432/gantt?schema=public"
PORT=3000
HOST=0.0.0.0
