# Dockerfile de DESARROLLO para el client (React + Vite).
# El código se monta como volumen (ver docker-compose.yml) para hot-reload.
FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev"]
